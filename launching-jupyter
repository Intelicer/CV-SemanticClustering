To launch the jupyter notebook CLIP-Project.ipynb you have to first download jupyter using pip: "pip install jupyter"
then you have to run the command jupyter notebook in the cmd terminal.
if "pip install jupyter" does not work, try using "python -m pip install jupyter" and "python -m jupyter notebook"
